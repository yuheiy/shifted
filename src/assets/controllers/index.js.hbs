export const definitions = [
{{#each dependencies}}
	import("./{{relativePath this.path}}").then((module) => ({
		identifier: "{{this.parsedPath.name}}",
		controllerConstructor: module.default,
	})),
{{/each}}
];
